<Page
    x:Class="SarasaGothic.LicensePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SarasaGothic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="_rootPanel" Background="{ThemeResource SystemControlPageBackgroundAltMediumBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="_titleBlock" Text="软件许可" Style="{ThemeResource TitleTextBlockStyle}" Margin="12,12,12,0" />
        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="_contentPanel" Margin="12" MaxWidth="640" HorizontalAlignment="Left">
                <controls:MarkdownTextBlock x:Name="_contentTextBlock" LinkClicked="MarkdownTextBlock_OnLinkClicked"
                                            Background="Transparent"/>
            </StackPanel>
        </ScrollViewer>
        <Grid x:Name="_bottomPanel" Grid.Row="2" Margin="12,0,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MaxWidth="160" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" MaxWidth="160" />
            </Grid.ColumnDefinitions>
            <Button IsEnabled="{x:Bind IsContentLoaded, Mode=OneWay}" Content="同意" Style="{ThemeResource AccentButtonStyle}" HorizontalAlignment="Stretch" Click="AgreeButton_Click" />
            <Button Content="拒绝并退出" HorizontalAlignment="Stretch" Grid.Column="2" Click="DisagreeButton_Click" />
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="_titleBlock.Margin" Value="24,12,24,0" />
                        <Setter Target="_contentPanel.Margin" Value="24,12" />
                        <Setter Target="_bottomPanel.Margin" Value="24,0,24,12" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1" />
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>

</Page>
