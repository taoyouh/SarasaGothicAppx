<Page
    x:Class="SarasaGothic.AppShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SarasaGothic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                Style="{StaticResource NavigationBackButtonNormalStyle}"
                IsEnabled="{x:Bind _contentFrame.CanGoBack, Mode=OneWay}"
                VerticalAlignment="Stretch"
                Click="BackButton_Click" />
            <Border x:Name="_appTitleBar" Grid.Column="1" Background="Transparent">
                <TextBlock x:Name="_appTitleTextBlock"
                    TextWrapping="NoWrap"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    VerticalAlignment="Center"
                    Margin="12,0,0,0" />
            </Border>
        </Grid>
        <Frame x:Name="_contentFrame" Grid.Row="1" />
    </Grid>
</Page>